# File upload configuration
# Directory where uploaded files will be stored (relative to project root)
FILE_UPLOAD_DIR=./uploadscd
# Max upload size in bytes (5MB default)
FILE_UPLOAD_MAX_BYTES=5242880
# Comma-separated allowed MIME types
FILE_UPLOAD_ALLOWED=image/jpeg,image/png,application/pdf

